---
"@ossintel/input-parser": minor
"@ossintel/github-normalizer": patch
"@app/web": patch
---

Introduce the `@ossintel/input-parser` package to isolate search query resolution across platforms. Refine the landing page search query parsing, add a syntax guide, support `/org/[orgname]` routing alongside `/user/[username]`, and implement client-side URL healing redirects.
